[project]
name = "ledger-service"
version = "0.1.0"
description = "Downstream — ledger-service (scaffold only, no business logic implemented)."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

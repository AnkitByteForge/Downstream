# infra/migrations

Cross-cutting, infra-level migrations that don't belong to a single service —
per Milestone 0 of the Implementation Blueprint: the artifact identity map,
the connector configuration store, and the initial seeded Graph Layer.

No migrations have been written yet. This folder is scaffold only.

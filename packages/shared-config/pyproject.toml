[project]
name = "shared-config"
version = "0.1.0"
description = "Downstream — shared-config (scaffold only, no business logic implemented)."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

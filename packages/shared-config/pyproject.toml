[project]
name = "shared-config"
version = "0.1.0"
description = "Downstream — shared configuration conventions used across services (ID format)."
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
test = ["pytest>=8"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
